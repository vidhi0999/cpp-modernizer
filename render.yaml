services:
  - type: web
    name: cpp-modernizer-api
    runtime: python
    buildCommand: bash build.sh
    startCommand: gunicorn api:app --bind 0.0.0.0:$PORT
    envVars:
      - key: GROQ_API_KEY
        sync: false